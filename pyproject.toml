[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "otto-demand-forecasting"
version = "1.0.0"
description = "Hourly cart and order forecasting from aggregated OTTO clickstream data"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Chaithanya Vemuri"}]
requires-python = ">=3.11"
dependencies = [
  "joblib>=1.4,<2",
  "matplotlib>=3.9,<4",
  "numpy>=2.0,<3",
  "pandas>=2.2,<3",
  "PyYAML>=6.0,<7",
  "scikit-learn>=1.5,<2",
  "torch>=2.4,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<10",
  "pytest-cov>=5,<8",
  "ruff>=0.9,<1"
]

[project.scripts]
otto-forecast = "otto_forecasting.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "--strict-markers"

[tool.ruff]
line-length = 96
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.coverage.run]
source = ["otto_forecasting"]
branch = true

[tool.coverage.report]
show_missing = true
fail_under = 65
