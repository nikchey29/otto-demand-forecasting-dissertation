# Resume Bullets

Use only values generated by the completed real-data research run. Replace bracketed fields and remove any claim not supported by the artifacts.

- Streamed and aggregated 220M+ e-commerce clickstream events into a validated UTC hourly series using memory-conscious JSONL processing and SHA-256 data fingerprinting.
- Designed leakage-aware expanding-window evaluation with an untouched final holdout, training-only scaling and repeated neural-network seeds for 24-hour cart and order forecasting.
- Benchmarked persistence, daily and weekly seasonal methods, Ridge, Extra Trees, GRU and Transformer models using MAE, RMSE, WAPE, MASE, RMSSE and horizon-level error analysis.
- Quantified model uncertainty with paired moving-block bootstrap comparisons and split-conformal prediction intervals, achieving [X%] empirical coverage at a 90% nominal level.
- Packaged the selected model with reproducible experiment manifests, automated tests, Docker and a FastAPI inference service.

Do not write “improved accuracy by X%” unless the comparison baseline is named and the generated statistical interval supports the claim.
